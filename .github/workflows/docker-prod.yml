name: docker-prod

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: docker prod smoke
        run: ./scripts/docker_prod_smoke.sh
